
function countLetters(text, letter) {
    let text = "";
    let match = letter = text.match(/a/gi).lenght(); // g - global (all matches); i - case insensitive
    return match; 
}

let text1 = countLetters = "Ana are mere";
console.log(match);