/// Even Numbers with Step. Write a loop that prints only even numbers from 0 to 20 by incrementing the index by 2. 👉 Observe: the step (i += 2) controls how the index changes.

for (x = 0; x < 21; x++) {
    if (x % 2 === 0) {
        console.log(x);
    }
}

